# Replace VITE_API_BASE_URL with the dev ALB domain once it is provisioned.
# Backend SecurityConfig must register this host as an allowed CORS origin.
VITE_API_BASE_URL=
VITE_USE_MOCKS=false
VITE_KAKAO_CLIENT_ID=
VITE_NAVER_CLIENT_ID=
